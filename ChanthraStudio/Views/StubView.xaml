<UserControl
    x:Class="ChanthraStudio.Views.StubView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource BrushBgBase}"
    d:DesignHeight="900"
    d:DesignWidth="1500"
    mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="540">
            <TextBlock
                HorizontalAlignment="Center"
                FontFamily="{StaticResource FontDisplay}"
                FontSize="38"
                FontStyle="Italic"
                Foreground="{StaticResource BrushGoldHi}"
                Effect="{StaticResource ShadowGoldGlowSoft}"
                Text="✦" />
            <TextBlock
                Margin="0,12,0,0"
                HorizontalAlignment="Center"
                FontFamily="{StaticResource FontDisplay}"
                FontSize="24"
                Foreground="{StaticResource BrushText1}"
                Text="{Binding Tag, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0} · coming next phase}" />
            <TextBlock
                Margin="0,8,0,0"
                HorizontalAlignment="Center"
                Style="{StaticResource TextSecondary}"
                FontSize="13"
                TextAlignment="Center"
                Text="The atelier is mid-build. Phase 1 ships the lunar shell, design system, and Generate workspace. Editor, Sound, and Node-flow follow." />
        </StackPanel>
    </Grid>
</UserControl>
