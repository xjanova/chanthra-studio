<UserControl
    x:Class="ChanthraStudio.Views.LibraryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ChanthraStudio.ViewModels"
    xmlns:controls="clr-namespace:ChanthraStudio.Controls"
    Background="{StaticResource BrushBgBase}"
    d:DataContext="{d:DesignInstance vm:LibraryViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="1500"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Floating toast (path copied / delete confirm / errors)  -->
        <Border
            Grid.Row="0"
            Grid.RowSpan="2"
            Margin="0,18,0,0"
            Padding="14,8"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Panel.ZIndex="50"
            CornerRadius="99"
            Effect="{StaticResource ShadowGoldGlowSoft}"
            Visibility="{Binding ToastMessage, Converter={StaticResource NotNullOrEmpty}, FallbackValue=Collapsed}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{StaticResource BrushSurface03}" />
                    <Setter Property="BorderBrush" Value="{StaticResource BrushLineHairline}" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ToastKind}" Value="ok">
                            <Setter Property="Background" Value="#1F6DBF8C" />
                            <Setter Property="BorderBrush" Value="#666DBF8C" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ToastKind}" Value="err">
                            <Setter Property="Background" Value="#1FD05A5A" />
                            <Setter Property="BorderBrush" Value="#66D05A5A" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock
                FontFamily="{StaticResource FontUI}"
                FontSize="13"
                Foreground="{StaticResource BrushText1}"
                Text="{Binding ToastMessage}" />
        </Border>

        <!--  Header bar  -->
        <Grid
            Grid.Row="0"
            Margin="32,22,32,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Style="{StaticResource TextPanelHeader}" FontSize="22">
                    <Run Foreground="{StaticResource BrushGold}" Text="✦ " />
                    <Run Text="Library" />
                </TextBlock>
                <TextBlock Margin="0,4,0,0" Style="{StaticResource TextSecondary}" FontSize="13.5" Text="{Binding Summary}" />
            </StackPanel>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                <!--  Render bar — visible only when something selected  -->
                <StackPanel Orientation="Horizontal" Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                    <Border Padding="10,6" VerticalAlignment="Center" Background="{StaticResource BrushSurface03}" BorderBrush="{StaticResource BrushLineGold}" BorderThickness="1" CornerRadius="99">
                        <TextBlock FontSize="12.5" Style="{StaticResource TextMonoUpper}" Foreground="{StaticResource BrushGoldHi}">
                            <Run Text="{Binding SelectedCount}" /><Run Text=" SELECTED" />
                        </TextBlock>
                    </Border>
                    <Button Margin="8,0,0,0" Command="{Binding ClearSelectionCommand}" Content="Clear" Style="{StaticResource ButtonPill}" />
                    <Button Margin="8,0,0,0" Command="{Binding DeleteSelectedCommand}" Content="Delete selected" Style="{StaticResource ButtonPill}"
                            ToolTip="Drop the selected clips + their files (confirmation required)" />
                    <Button Margin="8,0,0,0" Command="{Binding RenderFilmCommand}" Content="✦  Render film" Style="{StaticResource ButtonPillGold}" />
                    <Border Width="14" />
                </StackPanel>
                <!--  Kind chips — image / video / all  -->
                <StackPanel Margin="0,0,8,0" Orientation="Horizontal">
                    <RadioButton GroupName="libKind" Margin="0,0,2,0" Content="all" Style="{StaticResource ChipRadio}"
                                 Command="{Binding SetKindFilterCommand}" CommandParameter="all"
                                 IsChecked="{Binding KindFilter, Converter={StaticResource StringEquals}, ConverterParameter=all, Mode=OneWay}"
                                 ToolTip="Show both image and video clips" />
                    <RadioButton GroupName="libKind" Margin="0,0,2,0" Content="🖼 images" Style="{StaticResource ChipRadio}"
                                 Command="{Binding SetKindFilterCommand}" CommandParameter="image"
                                 IsChecked="{Binding KindFilter, Converter={StaticResource StringEquals}, ConverterParameter=image, Mode=OneWay}"
                                 ToolTip="Image clips only (PNG / JPG / WEBP …)" />
                    <RadioButton GroupName="libKind" Content="▶ videos" Style="{StaticResource ChipRadio}"
                                 Command="{Binding SetKindFilterCommand}" CommandParameter="video"
                                 IsChecked="{Binding KindFilter, Converter={StaticResource StringEquals}, ConverterParameter=video, Mode=OneWay}"
                                 ToolTip="Video clips only (MP4 / WEBM / MOV …)" />
                </StackPanel>

                <!--  Page-size chips — switch how many clips the next Refresh pulls  -->
                <StackPanel Margin="0,0,8,0" Orientation="Horizontal">
                    <RadioButton GroupName="libLimit" Margin="0,0,2,0" Content="50" Style="{StaticResource ChipRadio}"
                                 Command="{Binding SetLimitCommand}" CommandParameter="50"
                                 IsChecked="{Binding LimitMode, Converter={StaticResource StringEquals}, ConverterParameter=50, Mode=OneWay}"
                                 ToolTip="Show only the 50 newest clips" />
                    <RadioButton GroupName="libLimit" Margin="0,0,2,0" Content="200" Style="{StaticResource ChipRadio}"
                                 Command="{Binding SetLimitCommand}" CommandParameter="200"
                                 IsChecked="{Binding LimitMode, Converter={StaticResource StringEquals}, ConverterParameter=200, Mode=OneWay}"
                                 ToolTip="Default · 200 newest clips" />
                    <RadioButton GroupName="libLimit" Content="all" Style="{StaticResource ChipRadio}"
                                 Command="{Binding SetLimitCommand}" CommandParameter="all"
                                 IsChecked="{Binding LimitMode, Converter={StaticResource StringEquals}, ConverterParameter=all, Mode=OneWay}"
                                 ToolTip="Pull up to 1000 clips — slower if your library is large" />
                </StackPanel>
                <Button Margin="0,0,8,0" Command="{Binding SelectAllCommand}" Content="Select all" Style="{StaticResource ButtonPill}"
                        Visibility="{Binding HasClips, Converter={StaticResource BoolToVis}}" />
                <Button Command="{Binding RefreshCommand}" Content="Refresh" Style="{StaticResource ButtonPill}" />
            </StackPanel>
        </Grid>

        <!--  Body  -->
        <Grid Grid.Row="1">
            <!--  Empty state  -->
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding HasClips, Converter={StaticResource InvBoolToVis}}">
                <TextBlock
                    HorizontalAlignment="Center"
                    FontFamily="{StaticResource FontDisplay}"
                    FontSize="48"
                    Foreground="{StaticResource BrushGoldHi}"
                    Effect="{StaticResource ShadowGoldGlowSoft}"
                    Text="✦" />
                <TextBlock
                    Margin="0,12,0,0"
                    HorizontalAlignment="Center"
                    FontFamily="{StaticResource FontDisplay}"
                    FontSize="20"
                    FontStyle="Italic"
                    Foreground="{StaticResource BrushText1}"
                    Text="No clips yet" />
                <TextBlock
                    Margin="0,8,0,0"
                    HorizontalAlignment="Center"
                    Style="{StaticResource TextSecondary}"
                    FontSize="13.5"
                    TextAlignment="Center"
                    MaxWidth="380"
                    TextWrapping="Wrap"
                    Text="Generated images and videos will appear here. Hit ✦ Summon Scene in the Generate workspace to begin." />
            </StackPanel>

            <!--  Clip grid — masonry via WrapPanel. ListBox container + a
                  custom VirtualizingWrapPanel that the items panel uses give
                  us real row-band virtualisation (only the visible rows of
                  cards get realised; off-screen cards never instantiate
                  their BitmapImage). T52 · 7.21.  -->
            <ScrollViewer
                Padding="32,8,32,32"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto"
                CanContentScroll="True"
                Visibility="{Binding HasClips, Converter={StaticResource BoolToVis}}">
                <ItemsControl ItemsSource="{Binding Clips}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:VirtualizingWrapPanel ItemHeight="220" ItemWidth="280" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Margin="0,0,12,12"
                                Padding="0"
                                Cursor="Hand"
                                Style="{StaticResource SurfaceCard}">
                                <Border.InputBindings>
                                    <MouseBinding
                                        Command="{Binding DataContext.OpenClipCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        MouseAction="LeftDoubleClick" />
                                </Border.InputBindings>
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem
                                            Command="{Binding DataContext.OpenClipCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Header="Open in default viewer" />
                                        <MenuItem
                                            Command="{Binding DataContext.RevealCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Header="Reveal in Explorer" />
                                        <MenuItem
                                            Command="{Binding DataContext.CopyPathCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Header="Copy path" />
                                        <Separator />
                                        <MenuItem
                                            Command="{Binding DataContext.PostClipCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Header="✦ Post…" />
                                        <Separator />
                                        <MenuItem
                                            Command="{Binding DataContext.DeleteClipCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Header="Delete clip and file" />
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <!--  Thumb (file image, fallback grey)  -->
                                    <Border Grid.Row="0" CornerRadius="10,10,0,0" ClipToBounds="True">
                                        <Grid>
                                            <Border Background="{StaticResource BrushBgInset}" />
                                            <!--  Video / non-image fallback: a film-strip glyph + filename  -->
                                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    FontFamily="{StaticResource FontDisplay}"
                                                    FontSize="32"
                                                    Foreground="{StaticResource BrushGoldHi}"
                                                    Text="✦" />
                                                <TextBlock
                                                    Margin="0,4,0,0"
                                                    HorizontalAlignment="Center"
                                                    FontSize="11.5"
                                                    Style="{StaticResource TextMonoUpper}"
                                                    Text="MEDIA" />
                                            </StackPanel>
                                            <!--  Real image (only renders when ClipPathToImage returns non-null)  -->
                                            <Image
                                                RenderOptions.BitmapScalingMode="HighQuality"
                                                Source="{Binding FilePath, Converter={StaticResource ClipPathToImage}}"
                                                Stretch="UniformToFill" />
                                            <!--  Posted badge (top-right) — shown only when post_history has rows  -->
                                            <Border
                                                Margin="10"
                                                Padding="7,3"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Top"
                                                Background="#1F6DBF8C"
                                                BorderBrush="#666DBF8C"
                                                BorderThickness="1"
                                                CornerRadius="99"
                                                Effect="{StaticResource ShadowGoldGlowSoft}"
                                                Visibility="{Binding IsPosted, Converter={StaticResource BoolToVis}}">
                                                <TextBlock
                                                    FontFamily="{StaticResource FontMono}"
                                                    FontSize="11"
                                                    Foreground="{StaticResource BrushOk}"
                                                    Text="{Binding PostBadgeLabel}" />
                                            </Border>

                                            <!--  Selection checkbox overlay (top-left)  -->
                                            <CheckBox
                                                Margin="10"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Top"
                                                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                Cursor="Hand">
                                                <CheckBox.Template>
                                                    <ControlTemplate TargetType="CheckBox">
                                                        <Border
                                                            x:Name="bd"
                                                            Width="22"
                                                            Height="22"
                                                            Background="{StaticResource BrushBlackOverlay55}"
                                                            BorderBrush="{StaticResource BrushLineHairline}"
                                                            BorderThickness="1"
                                                            CornerRadius="4">
                                                            <Path
                                                                x:Name="check"
                                                                Width="11"
                                                                Height="11"
                                                                Data="M3 7 L7 11 L13 4"
                                                                Stretch="Uniform"
                                                                Stroke="#FF1A0D05"
                                                                StrokeThickness="2"
                                                                Visibility="Collapsed" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushGoldHi}" />
                                                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushGold}" />
                                                                <Setter TargetName="check" Property="Visibility" Value="Visible" />
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushLineGold}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </CheckBox.Template>
                                            </CheckBox>
                                        </Grid>
                                    </Border>
                                    <!--  Meta footer  -->
                                    <Border
                                        Grid.Row="1"
                                        Padding="12,8"
                                        BorderBrush="{StaticResource BrushLineHairline}"
                                        BorderThickness="0,1,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock
                                                    FontFamily="{StaticResource FontUI}"
                                                    FontSize="13"
                                                    FontWeight="Medium"
                                                    Foreground="{StaticResource BrushText1}"
                                                    Text="{Binding FileName}"
                                                    TextTrimming="CharacterEllipsis" />
                                                <StackPanel Margin="0,3,0,0" Orientation="Horizontal">
                                                    <TextBlock
                                                        FontSize="11.5"
                                                        Style="{StaticResource TextMonoUpper}"
                                                        Text="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" />
                                                    <TextBlock
                                                        Margin="6,0,0,0"
                                                        FontSize="11.5"
                                                        Style="{StaticResource TextMonoUpper}"
                                                        Foreground="{StaticResource BrushOk}"
                                                        Visibility="{Binding IsPosted, Converter={StaticResource BoolToVis}}"
                                                        Text="{Binding LastPostedLabel, StringFormat=· FB {0}}" />
                                                </StackPanel>
                                            </StackPanel>
                                            <Border
                                                Grid.Column="1"
                                                Padding="6,2"
                                                VerticalAlignment="Center"
                                                Background="{StaticResource BrushSurface03}"
                                                CornerRadius="4">
                                                <TextBlock FontSize="11" Style="{StaticResource TextMonoGold}" Text="{Binding SizeLabel}" />
                                            </Border>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
